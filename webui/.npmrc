# Disable minimumReleaseAge supply-chain policy that blocks
# recently published packages (e.g. @oxlint platform binaries).
# Without this, pnpm install fails during deployment because some
# binaries haven't aged past the default threshold yet.
minimum-release-age=0
